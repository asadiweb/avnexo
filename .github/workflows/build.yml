name: Auto Build Wiki

on:
  push:
    paths:
      - 'articles/**'
      - 'build.py'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: |
        pip install markdown pyyaml jinja2

    - name: Run build.py
      run: python build.py

    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v4
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./builds

        # انتهای build() قبل از print("Wrote index: ...")
cname_file = OUT_DIR / "CNAME"
cname_file.write_text("wiki.avnexo.com", encoding="utf-8")
print("Wrote CNAME for custom domain")
